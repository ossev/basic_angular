// Declaración e inicialización de una variable
const userName: string = 'nicobytes'

const sum = (a:number, b:number) => {
  return a +b;
}
sum (1,2)

class Person {
  constructor(public age:number, public lastName: string) {}
}

const os = new Person(32, 'Molina')